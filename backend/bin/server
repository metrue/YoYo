#!/usr/bin/env node --harmony_object_rest_spread

const Server = require('../src/server')
const CONFIG = require('../config.json')

const server = new Server(CONFIG)
server.start()
  .then(() => {
    console.log('YoYo api server is on')
  })
  .catch((e) => {
    console.log(e)
  })

